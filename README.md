# Megathon26 — Drone Detector

Drone vs non-drone detector from fixed-length baseband (I/Q) windows.

## Commands

```bash
pip install -r requirements.txt
python src/inspect_dataset.py
python src/train.py
python src/evaluate.py
python src/inference.py
```
